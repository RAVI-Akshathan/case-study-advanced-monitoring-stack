PowerShell automation script for downloading and silently installing the Netdata monitoring agent on Windows systems.

## Overview
This project contains a PowerShell script that automates the deployment of the Netdata agent on Windows servers.
The script downloads the latest Netdata MSI package from GitHub, performs a silent installation, uses configuration values supplied through environment variables, and records installation activity in a log file.
